# Storage directory for Astro Data

Due to the large size of astro data files is needed to manually copy from S3 bucket storage.

Run command:

`aws s3 cp s3://financial-astrology-data/ . --recursive`
